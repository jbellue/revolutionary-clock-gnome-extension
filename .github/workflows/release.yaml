name: Package Extension
on:
  push:
    tags:
      - 'v*'

jobs:
  lint:
    uses: ./.github/workflows/_lint-action.yaml

  package:
    needs: lint
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
      - name: Install dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y gettext make gnome-extensions-app
      - name: Build package
        run: make package
      - name: Upload to GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*.shell-extension.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      # Uncomment when ready to publish to extensions.gnome.org
      # - name: Upload to GNOME Extensions
      #   uses: murar8/gnome-extensions-action@0.1.0
      #   with:
      #     source-dir: src
      #     username: ${{ secrets.GNOME_USERNAME }}
      #     password: ${{ secrets.GNOME_PASSWORD }}
      #     accept-tos: true
