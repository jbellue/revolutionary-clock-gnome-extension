name: Reusable Lint

on:
  workflow_call:

jobs:
  eslint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'

      - name: Install ESLint
        run: npm install --global eslint@10

      - name: Run lint
        run: eslint --max-warnings=0 "src/**/*.js"